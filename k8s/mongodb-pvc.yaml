apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mongo-pvc
  namespace: sproj-app
spec:
  accessModes:
    - ReadWriteOnce  
  resources:
    requests:
      storage: 1Gi
  storageClassName: oci-bv  
