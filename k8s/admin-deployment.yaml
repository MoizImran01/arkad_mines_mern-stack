apiVersion: apps/v1
kind: Deployment
metadata:
  name: admin
  namespace: sproj-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: admin
  template:
    metadata:
      labels:
        app: admin
    spec:
      automountServiceAccountToken: false
      imagePullSecrets:
        - name: ghcr-secret
      containers:
        - name: admin
          image: ghcr.io/arkad-mines/admin:1.0.0
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 5173
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
              ephemeral-storage: "128Mi"
            limits:
              memory: "256Mi"
          env:
            - name: VITE_API_URL
              value: ""
